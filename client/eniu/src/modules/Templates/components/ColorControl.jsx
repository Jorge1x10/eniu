
import { useTranslation } from "react-i18next";export default function ColorControl({ id, label, value, error, originalValue, onChange }) {
  const { t } = useTranslation();

  return <div><div className="flex items-center justify-between gap-2"><label htmlFor={id} className="text-sm font-semibold text-[#2A2A2A]">{label}</label>{value !== originalValue && <button type="button" onClick={() => onChange(originalValue)} className="cursor-pointer text-xs font-semibold text-[#8A7420] underline">{t("Restaurar")}</button>}</div><div className="mt-2 flex gap-2"><input type="color" aria-label={t("Selector de {{name}}", { name: label.toLowerCase() })} value={/^#[0-9A-Fa-f]{6}$/.test(value) ? value : "#000000"} onChange={(event) => onChange(event.target.value.toUpperCase())} className="h-11 w-12 cursor-pointer rounded-lg border border-[#D9D9D9] bg-white p-1" /><input id={id} value={value} onChange={(event) => onChange(event.target.value)} aria-invalid={Boolean(error)} aria-describedby={error ? `${id}-error` : undefined} maxLength={7} className="min-h-11 min-w-0 flex-1 rounded-xl border border-[#D9D9D9] bg-white px-3 font-mono text-sm uppercase outline-none focus:border-[#E8C93D] focus:ring-2 focus:ring-[#FFE05A]/40" /></div>{error && <p id={`${id}-error`} className="mt-1 text-xs text-red-700">{error}</p>}</div>;
}
